# 실행 증빙 검토 및 수정 내용

검토 대상: GitHub main `c86a5a3` (git ignore로 인한 추가 푸시, 2026-09-11 한국시간 09:32:28). 이전 `436cc7c`에 추가된 자료까지 포함하여 `Security Project - 복사본/Model.pdf`, 프로그램의 `dist/evidence`, `dist/logs`, `dist/reports`를 재검토했다.
보고서 생성 시각: 2026-09-10 17:53:53. 이 문서는 저장된 실행 자료의 검토이며 현재 노트북 상태를 원격 점검한 결과가 아니다.

## 결론

기존 보고서의 '양호'를 전체 기준 충족 근거로 사용할 수 없다. PNG는 24개에서 52개로 늘었고, 추가 파일까지 확인했다. 64개 항목 중 12개는 PNG가 없으며, 로그상 11개는 서비스 생략 설정, W-18은 캡처 변수 초기화 오류가 원인이다. 누락을 일괄적으로 실행 실패 또는 보안 설정 취약으로 판단하지 않는다.

창 누적·포커스·배치 문제도 실제 원인이다. W-62와 W-64에는 앞서 열었던 보안 채널 설정이 남아 있고, W-52와 W-54는 같은 레지스트리 일부가 잡혀 있다. W-49와 W-58은 거의 빈 화면이다. 창 검색 결과를 검증하지 않고 전역 키보드 입력과 화면 좌표를 사용한 실행 흐름을 수정했다.

EXE 옆의 오래된 config를 로드하는 경로가 주요 원인이다. W-02 로그의 대상은 Administrator, W-08 보고서의 현재값은 True로, 수정한 소스 정책(Guest 대상, 잠금/초기화 기간 각각 60분)과 다르다. 기존 dist/config 및 dist/config/config 복사본을 새 빌드에서 정책 원본으로 사용하지 않도록 수정했다.

## 기존 등록 PNG 24개 재검토

'화면상 부합'은 해당 화면에 표시된 설정에 한정한다. 실제 변경을 프로그램이 수행했는지, GPO 유효 정책과 업무 기능까지 맞는지를 증명하지 않는다.

| 항목 | 관찰 및 판정 |
|---|---|
| W-01 | Admin_Secure로 이름 변경된 설정이 표시됨. 이름 변경 부분은 화면상 부합. SID 및 추가 계정 보호는 별도 확인 필요. |
| W-02 | Guest가 아닌 Administrator 계정 상태를 캡처. 증빙 불일치. |
| W-03 | 계정 목록이 잘려 보이지 않음. 불필요 계정 제거 여부 판단 불가. |
| W-04 | 잠금 임계값 5회. 화면상 부합. |
| W-05 | 해독 가능한 암호화 사용 안 함. 화면상 부합. |
| W-06 | Administrators가 아닌 Hyper-V Administrators 속성. 증빙 불일치. |
| W-07 | Everyone 권한을 익명 사용자에게 적용 사용 안 함. 화면상 부합. |
| W-08 | 잠금 기간 30분. Model.pdf PDF 14페이지(인쇄 185쪽)는 잠금 및 초기화 기간 각각 60분 이상. 화면 기준 미충족, 기존 양호 판정 부적절. |
| W-09 | 최소 암호 사용 기간 1일 등 일부 행만 표시. 복잡성·길이·최대 사용 기간·기억 개수 검증 불가. |
| W-10 | 마지막 로그온 사용자 이름 표시 안 함 사용. 화면상 부합. |
| W-11 | Guest, Users가 로컬 로그온 허용 목록에 포함. 승인된 업무 계정/그룹 검토 및 권한 정리 필요. 적합 판정 불가. |
| W-12 | 익명 SID/이름 변환 허용 사용 안 함. 화면상 부합. 다만 기존 점검값 1은 잘못된 대체 레지스트리 점검에서 나온 값이므로 근거로 사용 불가. |
| W-13 | 로컬 계정 빈 암호 사용 제한 사용. 화면상 부합. |
| W-14 | Remote Desktop Users 비어 있음. 원격 로그온 권한 및 관리자 계정까지 증명하지 않음. |
| W-15 | 정책 목록 일부만 잘려 표시되고 해당 개인키 보호 값 확인 불가. |
| W-16 | 내용이 없는 화면. 공유 및 NTFS ACL 증명 불가. |
| W-17 | 레지스트리 일부만 표시. AutoShare 값과 현재 실제 공유 목록을 확인할 수 없음. |
| W-20 | NetBIOS 설정 대신 Remote Desktop Users 속성. 로그에는 잘못된 WMI SettingID 조회 오류도 존재. |
| W-26 | 서비스 목록의 일부만 표시. RDS/MSADC 구성 확인 불가. 이전 커밋의 Remote Registry 속성 이미지와도 구분해야 함. |
| W-27 | Windows 11 버전 화면. 최신 업데이트 기준 대조 및 업데이트 절차 수립 여부는 확인 불가. 서버 가이드의 PC 적용 범위도 별도 검토 필요. |
| W-28 | 암호화 수준 정책 '사용' 표시. 상세 암호화 수준 값 자체가 보이지 않아 증빙 불충분. |
| W-35 | ODBC가 아닌 네트워크 연결 화면. 증빙 불일치. |
| W-36 | 세션 시간 제한 정책이 구성되지 않음으로 표시. 양호 판정을 뒷받침하지 못하며 유효 시간 제한 재점검 필요. |
| W-37 | 작업 스케줄러 목록과 한 작업의 일반 정보. 모든 작업의 실행 명령·인수·인가 여부를 보여주지 않아 의심 명령 부재를 증명하지 못함. |

## 추가 PNG 28개 검토

| 항목 | 관찰 및 판정 |
|---|---|
| W-23 | 익명으로 액세스할 수 있는 공유 목록은 비어 있음. 익명 접근 제한 전체 조건을 이 한 화면만으로 확정할 수 없음. |
| W-38 | Windows 업데이트의 최신 상태 안내. 벤더별 보안 패치, 주기 및 적용 절차 전체는 별도 확인 필요. |
| W-39 | Windows 보안 요약 화면. 백신 정의 버전과 업데이트 시각 확인 불가. |
| W-40 | 계정 로그온 이벤트 감사의 성공·실패가 선택됨. 필요한 전체 감사 범주와 조직 정책까지 증명하지 않음. |
| W-41 | 날짜 및 시간 일반 탭. NTP 서버와 마지막 동기화 성공 여부 확인 불가. |
| W-42 | 이벤트 뷰어 목록. 로그별 최대 용량과 보존·덮어쓰기 정책 증명 불가. |
| W-43 | Logs 폴더 일반 탭. 보안 ACL이 보이지 않음. |
| W-44 | 서비스 목록. 원격 레지스트리 서비스 및 허용 경로의 상세 상태 확인 불가. |
| W-45 | Windows 보안 개요. 백신 공급자·실행 상태 상세 검증 필요. |
| W-46 | SAM 파일 일반 탭. 접근 권한이 보이지 않음. |
| W-47 | 3차원 텍스트 화면보호기, 대기 10분. '다시 시작할 때 로그온 화면 표시'가 해제됨. 화면 기준 암호 보호 조건 미충족. |
| W-48 | 로그온하지 않고 시스템 종료 허용 사용 안 함. 화면상 부합. |
| W-49 | 내용이 보이지 않는 잘린 화면. 원격 종료 권한 확인 불가. |
| W-50 | 보안 감사 기록 불가 시 즉시 종료 사용 안 함. 화면상 부합. |
| W-51 | SAM 계정과 공유의 익명 열거 금지 사용. 화면상 부합. |
| W-52 | 관련 없는 레지스트리 일부. AutoAdminLogon 값을 확인할 수 없음. |
| W-53 | 이동식 미디어 포맷/꺼내기 허용 대상 Administrators. 화면상 부합. |
| W-54 | W-52와 같은 레지스트리 일부. DoS 방어의 OS별 적용 조건 확인 불가. |
| W-55 | 사용자가 프린터 드라이버를 설치할 수 없게 함 사용. 화면상 부합. |
| W-56 | 로그온 시간 만료 시 연결 끊기는 사용. 추가 조건인 유휴 15분 이하 설정은 확인 불가. |
| W-57 | 정책 목록 일부만 표시. 경고 제목과 내용이 보이지 않음. 이전 파일명은 두 하위 항목을 덮어썼음. |
| W-58 | 내용이 없는 화면. 사용자 홈 디렉터리 권한 확인 불가. |
| W-59 | NTLMv2만 보내고 LM/NTLM을 거부하도록 표시. 화면상 부합. |
| W-60 | 가능한 경우 디지털 서명 한 가지가 사용됨. 필요한 세 정책 전체를 확인할 수 없음. |
| W-61 | C: 볼륨 NTFS 표시. C:에 한정하여 부합하며 다른 고정 볼륨은 별도 확인 필요. 기존 수동 설명의 ACL 내용은 Model.pdf의 NTFS 기준과 달라 수정함. |
| W-62 | 시작 프로그램 목록 대신 보안 채널 서명 설정. 증빙 불일치. |
| W-63 | 서비스 목록. Kerberos 시계 오차 5분 이하 정책 증명 불가. |
| W-64 | 방화벽 대신 보안 채널 서명 설정. 증빙 불일치. |

PNG 미제공 항목: W-18, W-19, W-21, W-22, W-24, W-25, W-29, W-30, W-31, W-32, W-33, W-34 (12개).
W-18 로그의 `app_name` 초기화 오류를 수정했다. 나머지 11개는 서비스 생략 선택이 로그에 남아 있으며, 서비스가 실제 불필요하거나 미설치임을 검증한 증거는 아니다.

## 수정된 실행 흐름

스캔이 없으면 실행 버튼에서 스캔으로 연결한다. 초기 옵션 및 필요한 운영 영향 항목을 한 번에 확인한 다음, 항목별로 조치 → 화면 캡처 → 이번 항목의 창 정리 → 값 재조회 및 증빙 JSON 저장 → 다음 항목으로 진행한다.

- EXE 내부에 빌드 시 config를 포함하고 내부 리소스에서 정책과 보고서 템플릿을 읽는다. EXE 옆의 낡은 config는 사용하지 않는다.
- 실행마다 evidence/날짜-실행ID 폴더를 만든다. 이전 실행 이미지와 섞이지 않는다.
- JSON에 PC 이름, 시각, 정책 해시, 전후 값, 조치 결과, 백업 경로, 캡처 파일 해시, 오류를 기록한다. PNG 저장 자체를 적합 판정으로 사용하지 않는다.
- W-57_Caption 및 W-57_Text 증빙 이름을 분리한다.
- 대상 이름은 정확히 비교하여 Hyper-V Administrators를 Administrators로 오인하지 않는다. 팝업은 대상 관리 프로세스와 연결해 찾는다.
- 관리 콘솔은 실행한 MMC 프로세스 PID로 찾는다. 속성 창은 소유 관계까지 확인한다. 같은 Explorer 프로세스의 관계없는 창은 닫지 않는다.
- 증빙 창을 좌측에 배치한 뒤 Win32 창 좌표를 다시 읽고 캡처 직전·직후 전경 창을 검사한다. UI Automation의 잘못된 영역 좌표에만 의존하지 않는다.
- 전역 방향키 입력을 제거했다. 필요한 경우 대상 콘솔에 포커스가 있는 것을 확인한 후 그 목록에서만 키보드 탐색한다.
- W-08 두 설정, W-09 다섯 설정, W-56 두 설정, W-60 세 설정은 각각 PNG로 저장한다. 부분 실패 시 이미 저장된 이미지도 JSON에 기록한다.
- Logs/SAM 파일 속성은 일반 탭 대신 보안 탭을 선택한다. NIC 설정은 전체 NIC 원시 값을 JSON으로 저장하며 연결 목록 PNG만으로 설정 완료를 판정하지 않는다.
- 로그의 항목 ID를 메시지 발생 시점에 고정하고 실행별 폴더에 저장한다. UI 콜백 지연 때문에 다음 항목 로그가 이전 파일에 들어가거나 과거 로그가 삭제되는 문제를 수정했다.
- 잘린 캡처, 대상 미확인, 다른 제목의 대화상자를 성공으로 처리하지 않는다. 자동 창 정리가 실패하면 다음 항목 진행을 중단한다.
- Markdown 및 Excel의 실행 증빙 시트에서 보안 판정과 캡처 결과를 분리한다. 중단 시에도 부분 결과 보고서를 생성한다.
- 서비스 사용 여부가 확인되지 않았다는 이유만으로 미사용으로 간주하여 생략하지 않는다.

## 추가 자동화 범위와 한계

W-02는 EnableGuestAccount=0, W-12는 LSAAnonymousNameLookup=0을 Secedit의 개별 키 적용 경로로 처리한다. 변경 전 백업, 재조회 검증, 실패 시 복원 경로를 사용한다. 기존 코드의 도메인/GPO 보호 조건은 유지한다.

추가 자동 조치:

| 항목 | 적용 및 검증 | 범위·영향 |
|---|---|---|
| W-47 | 화면보호기 활성화, 해제 시 암호, 600초 이하, 유효한 화면보호기 파일의 네 설정을 백업·적용·재조회. 부분 실패 시 원래 값 또는 미설정 상태 복원. | 현재 실행 계정 HKCU 범위. 무인 관제 화면 잠금 가능. 다른 사용자 전체에 적용했다고 판정하지 않음. 재로그온 후 실제 잠금 시험 필요. |
| W-56 | SMB API로 로그온 시간 만료 연결 끊기와 지원되는 유휴 시간 설정을 함께 적용. 기존의 더 짧은 유휴 시간은 보존. | 공유/백업/관제 세션 유지 요구 확인. API가 노출되지 않는 OS에서는 오류로 남기고 변경하지 않음. |
| W-64 | 세 방화벽 프로필 활성화. 원래 프로필 값을 백업하고 PersistentStore와 ActiveStore 양쪽 재조회. 실패하면 원래 값 복원 시도. | 기존 허용 규칙은 유지. 허용 규칙 없는 서비스 통신이 차단될 수 있어 시작 시 일괄 운영 영향 확인 대상에 포함. |

세 항목 모두 기존과 같은 도메인/GPO 변경 제한과 운영 영향 확인을 거친다. 제한을 해제해서 도메인 정책을 무조건 덮어쓰는 방식은 구현하지 않았다.

W-03 로컬 계정, W-06 관리자 그룹, W-20 NIC별 NetBIOS, W-40 감사 정책, W-42 이벤트 로그 설정은 원시 값을 자동 수집한다. W-61은 문자 할당된 고정 드라이브의 NTFS/FAT를 자동 점검하지만 디스크 변환은 하지 않는다. 이들은 자동 수집·점검이며 자동 삭제/변환은 아니다.

현재 정책 파일 65개(64개 기준, W-57 두 하위 항목) 중 자동 변경 경로는 24개다. Type_Skip 39개, 기타 읽기 전용 점검 2개가 남아 있어 전체 기준의 무인 자동 조치는 완성되지 않았다. 계정·공유·서비스의 업무상 필요성, 허용할 NIC 및 접속 대역, 업무별 방화벽 규칙, 재부팅 허용 시간 등은 이 저장소만으로 결정할 수 없다. 예를 들어 CryptSvc나 Spooler를 일괄 중지하면 인증서/업데이트 또는 인쇄 업무에 영향이 생길 수 있어 '불필요'로 추측해서 제거하지 않는다. 이 항목들은 장비별 승인된 설정 목록을 받아 자동화 범위를 확장해야 한다.

Windows가 전경 창 전환을 거절할 수 있으므로 실패를 검증하고 진행을 멈추거나 캡처 오류로 기록한다. 구현 참고: [SetForegroundWindow](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setforegroundwindow), [SMB 설정 API](https://learn.microsoft.com/en-us/powershell/module/smbshare/set-smbserverconfiguration), [방화벽 프로필 API](https://learn.microsoft.com/en-us/powershell/module/netsecurity/set-netfirewallprofile), [화면보호기 실행 파일 정책](https://learn.microsoft.com/en-us/windows/win32/devnotes/scrnsave-exe).

## 빌드와 확인

Windows에서 KISA-AutoPatcher-Python 폴더의 PowerShell에서 실행:

```powershell
python -m pip install -r requirements.txt pyinstaller
.\build.ps1
```

실행 정책으로 ps1이 차단되면 같은 폴더에서 다음 명령을 직접 실행한다:

```powershell
python -m PyInstaller --clean --noconfirm --onefile --noconsole --name KISA-AutoPatcher --add-data "config;config" --collect-all customtkinter --collect-all uiautomation main.py
```

생성물: dist/KISA-AutoPatcher.exe. 새 실행 결과: dist/evidence/날짜-실행ID/ 아래 PNG·JSON·reports.

이번 검증은 Linux에서 자동 테스트 52개 통과, Python 문법 검사 및 git diff --check로 수행했다. 테스트에는 포커스 전환 중 캡처 거부, 항목 간 창 종료 순서, 무관한 창 보존, 부분 변경 실패 시 복원, 방화벽 실효 설정 불일치 시 복원이 포함된다. 실제 Windows GUI, Secedit 적용/복원 및 EXE 빌드는 이 환경에서 실행하지 못했다. 먼저 테스트 장비에서 새 EXE를 실행하여 W-02의 Guest 창, W-08의 두 60분 값, W-12의 사용 안 함, 항목 간 창 종료, JSON 및 보고서의 일치 여부를 확인해야 한다.
