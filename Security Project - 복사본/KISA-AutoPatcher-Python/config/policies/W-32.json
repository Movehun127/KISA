{
    "Title": "DNS 서비스 구동 점검",
    "Category": "서비스 관리",
    "Level": "상",
    "TechType": "Type_Powershell",
    "CheckCommand": "$s = Get-Service -Name DNS -ErrorAction SilentlyContinue; if ($s -eq $null -or $s.Status -ne 'Running') { 'True' } else { 'False' }",
    "RemediationCommand": "Stop-Service -Name DNS -Force -ErrorAction SilentlyContinue; Set-Service -Name DNS -StartupType Disabled -ErrorAction SilentlyContinue",
    "SecureValue": "True",
    "ItemId": "W-32",
    "appTarget": "services.msc",
    "treePath": [],
    "targetItem": [
        "DNS Server",
        "DNS"
    ]
}